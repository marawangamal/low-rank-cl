name: low-rank-cl
channels:
  - pytorch
  - nvidia
  - defaults

dependencies:
  - python=3.9
  - pytorch=2.0.1
  - torchvision=0.15.2
  - pytorch-cuda=11.8
  - pip

  - pip:
      - six
      - ipdb
      - scipy
      - pyyaml
      - tqdm
      - timm==0.6.12
      - numpy==1.25.2
      - scikit-learn==1.2.0